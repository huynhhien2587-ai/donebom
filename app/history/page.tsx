"use client";
import {useEffect,useState} from "react";import{supabase}from"@/lib/supabase";
export default function History(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data:{session}}=await supabase.auth.getSession();if(!session){location.href="/auth";return}const r=await fetch("/api/history",{headers:{Authorization:`Bearer ${session.access_token}`}});const j=await r.json();setItems(j.items||[])})()},[]);return <main className="app"><header className="topbar"><div className="brand">BOM <span>FILTER</span><em>V3.1</em></div><button className="all" onClick={()=>location.href="/"}>← BOM</button></header><section className="results"><div className="step"><span>03</span> LỊCH SỬ BOM</div><div className="grid">{items.map(x=><div className="group" key={x.token}><div className="gtext"><strong>{x.filename}</strong><small>{new Date(x.created_at).toLocaleString("vi-VN")} · {x.result?.total??0} dòng</small></div></div>)}</div>{!items.length&&<p>Chưa có lịch sử.</p>}</section></main>}
